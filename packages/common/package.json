{
  "name": "@caglarturali/piyango-common",
  "version": "5.2.0",
  "main": "cjs/index.js",
  "module": "esm/index.js",
  "types": "esm/index.d.ts",
  "repository": "git://github.com:caglarturali/piyango.git",
  "scripts": {
    "clean": "rimraf ./cjs ./esm",
    "prebuild": "pnpm run clean",
    "build": "pnpm run build:cjs && pnpm run build:esm",
    "build:cjs": "tsc -p tsconfig.build.json --module commonjs --outDir cjs",
    "build:esm": "tsc -p tsconfig.build.json --module esnext --outDir esm",
    "test": "echo \"Error: no test specified\" && exit 1",
    "prepack": "pnpm run build",
    "sloc": "node-sloc . -x node_modules,cjs,esm"
  },
  "dependencies": {
    "@caglarturali/piyango-utils": "^0.3.0",
    "moment": "^2.24.0"
  },
  "devDependencies": {
    "@types/node": "^13.11.0",
    "typescript": "^3.8.3"
  },
  "files": [
    "cjs",
    "esm",
    "src"
  ],
  "keywords": [],
  "author": "",
  "license": "ISC"
}
