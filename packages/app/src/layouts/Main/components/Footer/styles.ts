/**
 * Footer component styles.
 */
import { createStyles, Theme } from '@material-ui/core';

export default ({ spacing }: Theme) =>
  createStyles({
    root: {
      padding: spacing(4),
    },
  });
